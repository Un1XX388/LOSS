<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LOSS;assembly=LOSS"
             x:Class="LOSS.TestXAML"
             Title="test list - matthew">
  <ListView x:Name="TestView"
            SelectedItem="{Binding Source={x:Reference SomeLabel},Path=Text}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <TextCell Text="{Binding DisplayName}"
                  TextColor="{x:Static local:Colors.purple}"/>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>